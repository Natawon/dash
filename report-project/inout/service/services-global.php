<?php
    // require_once("services.php");
    // $configuration = configuration();
    // $groups = groups($groupKey);
    // $categories = categories($groupKey);
    // $members = session();

    // if (!$groups) {
    //     header('Location: '.constant("_PAGE_404"));
    // } else if ($groups['status'] != 1) {
    //     header('Location: '.constant("_PAGE_404"));
    // }
?>